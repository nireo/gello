export interface CreateTemplate {
  title: string;
  description: string;
  lists: string;
  private: string;
}

export interface Template {
  title: string;
  description: string;
  lists: string;
  created_at: string;
}
