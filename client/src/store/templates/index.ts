const initialState = {
  userTemplates: [],
  templates: [],
};

const reducer = (state: any = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
